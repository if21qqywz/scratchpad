# Data Contracts Notes

- Data contracts = shared agreements between producers/consumers (schema, SLA, semantics)
- Start small: one critical dataset, not all tables
- Key fields: schema, ownership, freshness, quality checks
- Tools: Great Expectations, dbt tests, OpenMetadata
- From meetup: "A contract is only useful if someone reads it" — put contract next to schema, not wiki
