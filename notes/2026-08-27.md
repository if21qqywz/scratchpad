# 2026-08-27

Quick thought: for the daily ingestion job, consider partitioning by `ingest_date` instead of `event_time` to simplify backfills and idempotent retries. Also worth checking whether the Parquet file size stays around 128MB after compression.
